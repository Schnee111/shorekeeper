Selesaikan SEMUA FASE Shorekeeper (FASE 1 FOUNDATION → FASE 2 MULTI-AGENT → FASE 3 PRODUCTION) secara berurutan sesuai /home/daffa/projects/shorekeeper/TASKS.md.

Prioritas: kerjakan task P0 dulu dalam tiap fase, baru P1. Maksimal 5 worker paralel (RAM WSL 7.8GB). Setelah FASE N gate pass → otomatis lanjut FASE N+1 tanpa konfirmasi. Jika blocked → tulis docs/BLOCKERS.md dengan: (1) yang dicoba, (2) error persis, (3) yang dibutuhkan → LANJUT ke fase berikutnya yang tidak depend, atau pause jika semua depend.

verify: bash scripts/gates/gate-fase1.sh && bash scripts/gates/gate-fase2.sh && bash scripts/gates/gate-fase3.sh

constraints: jangan tambah dependency berbayar; ikuti layout monorepo yang sudah ditentukan di TASKS.md (apps/agent, apps/orchestrator, apps/token-server, apps/client, packages/contracts, packages/omp-bridge, packages/task-store); jangan ubah arsitektur yang sudah locked (LiveKit+Gemini 3.1 supervisor, Hermes orchestrator, oh-my-pi worker, SQLite WAL task store); gunakan model yang sudah dikonfigurasi (main qd/kmodel_latest, subagent ag/gemini-3.7-flash-high, goal_judge qd/kmodel_latest)

boundaries: hanya file di dalam /home/daffa/projects/shorekeeper (kecuali ~/.omp/agent/models.yml untuk konfigurasi model worker yang sudah dibuat)

stop when: (a) semua 3 fase selesai dan gate pass, (b) blocked total yang tidak bisa di-recover, atau (c) budget turns habis. Jangan stop hanya karena satu fase blocked — catat dan lanjut.

Laporkan di pesan terakhir: status per fase (done/blocked/in-progress), bukti gate pass per fase (output command exit 0 + path file yang dibuat), dan hasil akhir `bash scripts/gates/gate-fase3.sh` (atau status terakhir jika tidak sampai).
